# ===========================================
# BFP Berong - Environment Configuration
# Updated: December 2025
# ===========================================
# Copy this file to .env and fill in your values

# ===========================================
# Database Configuration (PostgreSQL)
# ===========================================

# For Docker deployment:
DATABASE_URL="postgresql://bfp_user:bfp_secret_password@postgres:5432/bfp_berong?schema=public"

# For local development with PostgreSQL:
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/berong?schema=public"

# Docker PostgreSQL credentials (used by docker-compose)
POSTGRES_USER=bfp_user
POSTGRES_PASSWORD=bfp_secret_password
POSTGRES_DB=bfp_berong

# ===========================================
# Backend URL (Python FastAPI simulation server)
# ===========================================
BACKEND_URL=http://localhost:8000

# For Docker:
# BACKEND_URL=http://python-backend:8000

# ===========================================
# Authentication
# ===========================================
# JWT Secret (generate a random string for production)
# You can generate one with: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# ===========================================
# AI/ML Features (Google Gemini)
# ===========================================
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key

# ===========================================
# CORS Configuration (for production)
# ===========================================
CORS_ORIGINS=http://localhost:3000,http://localhost

# ===========================================
# Email / SMTP Configuration (Password Reset & Notifications)
# ===========================================
# For Gmail: use an App Password (https://myaccount.google.com/apppasswords)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=stacruzfs027@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=BFP SafeScape <stacruzfs027@gmail.com>
